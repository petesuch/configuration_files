
#### Commands to Pay attention to while reading the ArchLinux Installation Wiki

# ls /usr/share/kbd/keymaps/**/*.map.gz
# ls /sys/firmware/efi/efivars
# timedatectl set-ntp true
# fdisk -l

# fdisk /dev/sda ... do your thing
# mkfs.ext4 /dev/sda1
# mkfs.ext4 /dev/sda3
# mkswap /dev/sda2
# swapon /dev/sda2

# reflector --latest 200 --protocol http --protocol https --sort rate --save /etc/pacman.d/mirrorlist

# mount /dev/sda3 /mnt
# mkdir /mnt/boot
# mount /dev/sda1 /mnt/boot

# pacstrap /mnt linux linux-firmware base

# genfstab -U /mnt >> /mnt/etc/fstab

# arch-chroot /mnt 

# pacman -Syy grub vim sudo

# grub-install /dev/sda
# grub-mkconfig -o /boot/grub/grub.cfg

# pacman -Syy btfrfs-progs 
# pacman -Syy virtualbox-guest-utils
# pacman -Syy samba


# systemctl start systemd-networkd
# systemctl start systemd-resolved

# systemctl enable systemd-networkd
# systemctl enable systemd-resolved

# systemctl start sshd
# systemctl enable sshd

# reflector --latest 200 --protocol http --protocol https --sort rate --save /etc/pacman.d/mirrorlist

## Don't forget to add stuff NOW... the new system does not have pacman??

# pacman -Sy grub openssh vim gcc bison python3 pacman...

# ip link set dev enp0s3 up
